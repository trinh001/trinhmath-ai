# TrinhMath AI — Current automated development task

Paste ONE substantial coding package below, then start it with `RUN_DEV_TASK_AUTO.bat`.

The external launcher runs Codex first. If Codex exits because quota/rate-limit
is exhausted, DeepSeek through Aider continues from the SAME git working tree.
Do not launch Codex directly for tasks where automatic fallback is required.

## TASK

Replace this section with the complete task package.
